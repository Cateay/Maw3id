const {supabase,cors}=require('./_lib');module.exports=async(req,res)=>{cors(res);if(req.method==='OPTIONS')return res.status(204).end();let date=req.query.date;if(!/^\d{4}-\d{2}-\d{2}$/.test(date))return res.status(400).json({error:'تاريخ غير صحيح'});let {data,error}=await supabase().from('bookings').select('start_time').eq('session_date',date).neq('attendance_status','declined');if(error)return res.status(500).json({error:'تعذر تحميل المواعيد'});res.json({taken:(data||[]).map(x=>String(x.start_time).slice(0,5))})}
